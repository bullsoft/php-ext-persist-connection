// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("fixapi", "for fixapi support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("fixapi", "enable fixapi support", "no");

if (PHP_FIXAPI != "no") {
	EXTENSION("fixapi", "fixapi.c");
}

